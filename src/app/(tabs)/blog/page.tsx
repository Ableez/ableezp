import BlogCard from "@/app/_components/about/blog-cards";
import React from "react";

const BlogPage = () => {
  return (
    <div className="min-h-screen">
      <BlogCard length={10} />
    </div>
  );
};

export default BlogPage;
